'use strict'

const {
  formatCard,
  formatListCards,
} = require('./format-card');

const {
  formatResponse
} = require('./format-response');

module.exports = {
  formatCard,
  formatListCards,
  formatResponse
}
